import BinaryBuffer from "./BinaryBuffer";

/**
 * Provides the tree structure for 
 */
class ElementTree {
    private static _fromBinary(in: BinaryBuffer, lookup: StringLookup, parent?: Element): [Map<string, Element>, string] {

    }
        
    private _elements: Map<string, Element> = new Map();

    static fromBinary(in: BinaryBuffer, lookup: StringLookup, ): ElementTree {

    }

    static fromJSON(json: any): ElementTree {

    }

    constructor() {

    }

    
    toBinary(out: BinaryBuffer, lookup: StringLookup) {

    }

    toJSON(): any {

    }
}